{
  "name": "mathdeck-keyboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "check": "node --check popup.js",
    "test": "npm run build && node --test"
  },
  "dependencies": {
    "mathlive": "^0.110.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
