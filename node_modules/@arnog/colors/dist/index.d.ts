/**
 * Color represented as a hex string.
 *
 * Supports both RGB and RGBA formats:
 * - RGB: "#RRGGBB" (e.g., "#FF0000" for red)
 * - RGBA: "#RRGGBBAA" (e.g., "#FF000080" for red at 50% opacity)
 *
 * The alpha channel (AA) is optional and represents opacity from
 * 00 (fully transparent) to FF (fully opaque).
 */
type HexColor = string;
type RgbColor = {
    r: number;
    g: number;
    b: number;
    alpha?: number;
};
type OklchColor = {
    L: number;
    C: number;
    H: number;
    alpha?: number;
};
type OklabColor = {
    L: number;
    a: number;
    b: number;
    alpha?: number;
};
type Color = HexColor | RgbColor | OklchColor | OklabColor;

/** sRGB gamma correction (linear -> sRGB). Handles negative values via sign preservation. */
declare function gammaCorrect(channel: number): number;
/** Inverse sRGB gamma correction (sRGB -> linear). Handles negative values via sign preservation. */
declare function inverseGammaCorrect(channel: number): number;
declare function hslToRgb(h: number, s: number, l: number): {
    r: number;
    g: number;
    b: number;
};
/** Convert an RGB color (0-255 per channel) to HSL.
 * Returns h: 0-360, s: 0-1, l: 0-1. */
declare function rgbToHsl(r: number, g: number, b: number): {
    h: number;
    s: number;
    l: number;
};
/**
 * Convert an HSV color to RGB. Hue is in degrees and wraps modulo 360;
 * saturation and value are clamped to [0, 1]. Returns r/g/b in [0, 255]
 * as floats — not rounded, so the precision is preserved for downstream
 * conversions (e.g. into OKLab/OKLCh).
 */
declare function hsvToRgb(h: number, s: number, v: number): {
    r: number;
    g: number;
    b: number;
};
/** Convert an RGB color (0-255 per channel) to HSV.
 * Returns h: 0-360, s: 0-1, v: 0-1. */
declare function rgbToHsv(r: number, g: number, b: number): {
    h: number;
    s: number;
    v: number;
};
/**
 * Convert 0-255 RGB components to a `#rrggbb` hex string.
 */
declare function rgbToHex(r: number, g: number, b: number): HexColor;
/** Parse a hex color string (#rgb, #rrggbb, #rrggbbaa) to an RgbColor. */
declare function parseHexColor(s: string): RgbColor;
declare function asOklch(color: HexColor | RgbColor | OklchColor | OklabColor): OklchColor;
declare function asRgb(color: number | Color): RgbColor;
declare function asColorNumber(color: number | Color): number;
declare function colorToHex(color: number): string;
declare function oklchToOklab(_: OklchColor): OklabColor;
declare function oklabToOklch(_: OklabColor): OklchColor;
/** Convert an oklab color to sRGB, clipping the chroma if necessary */
declare function oklabToRgb(color: OklabColor): RgbColor;
declare function oklchToRgb(_: OklchColor): RgbColor;
declare function rgbToOklab(_: RgbColor): OklabColor;
declare function rgbToOklch(_: RgbColor): OklchColor;
declare function cMaxFor(L: number, Hdeg: number): number;
/**
 * Convert OKLCH to RGB color
 * @param l lightness: 0..100
 * @param c chroma: 0..about 0.4
 * @param h hue: 0..360
 * @returns 0xrrggbbaa color
 */
declare function oklch(l: number, c: number, h: number): number;
/**
 * Apply the per-shade dark-mode OKLCH shift to a packed color.
 * Shifts lightness +2.8, chroma +0.002, hue +0.46.
 */
declare function toDarkColor(color: number): number;
declare function oklchFromRGB(rgba: number): {
    l: number;
    c: number;
    h: number;
};

/**
 * Return true if `s` is a named color from the NAMED_COLORS palette
 * (e.g. "red", "blue", "dark-grey") or "transparent".
 */
declare function isNamedColor(s: string): boolean;
/**
 * Given a string `s` in the following formats, return a 0xrrggbbaa color.
 * - hex: #rrggbbaa, #rgb, #rrggbb
 * - rgba: rgba(r, g, b, a)
 * - rgb: rgb(r, g, b) or rgb(r, g, b / a) or rgb(r g b / a) or rgb(r g b)
 * - oklch: oklch(l c h / a) or oklch(l c h) or oklch(l, c, h / a) or oklch(l, c, h), when l is a percentage, c is a number between 0 and 0.4, and h is a number between 0 and 360, for example "oklch(50% 0.3 240 / 0.8)" or "oklch(50% 0.3 240 / 80%)"
 * - oklab: oklab(L a b / alpha) or oklab(L a b), where L is 0-1 (or percentage), a is ~-0.4 to 0.4, b is ~-0.4 to 0.4
 * - hsl: hsl(h, s, l) or hsl(h, s, l / a) or hsl(h s l / a) or hsl(h s l), where h is a number between 0 and 360, s is a percentage, l is a percentage and a is a percentage
 * - named: color names from the NAMED_COLORS palette (e.g. "red", "blue", "cyan", "dark-grey") or "transparent"
 * - palette: "{name}-{shade}" from COLOR_SCALE_PRESETS (e.g. "red-700", "blue-300"). When `darkMode` is true, each shade is shifted via `toDarkColor()` (per-shade OKLCH adjustment).
 * - opacity suffix: any of the above followed by "/<0–100>" (e.g. "blue-500/66", "#ff0000 / 50%", "red/80")
 */
declare function parseColor(s: string, darkMode?: boolean): number;
declare function parseColorToRgb(s: string, darkMode?: boolean): RgbColor;
declare function parseColorToHex(s: string, darkMode?: boolean): HexColor;
/**
 * Parse any color string to a `[0-1, 0-1, 0-1]` float tuple.
 * Useful for WebGL uniforms that expect normalized RGB values.
 */
declare function parseColorToRgb01(s: string, darkMode?: boolean): [number, number, number];
/**
 * Parse any color string to a `[0-1, 0-1, 0-1, 0-1]` float tuple (RGBA).
 * Useful for WebGL uniforms that need both color and alpha.
 */
declare function parseColorToRgba01(s: string, darkMode?: boolean): [number, number, number, number];
/**
 * Set the alpha channel of a color string.
 *
 * @returns `#rrggbbaa` hex string (or `#rrggbb` if alpha is 1.0)
 */
declare function parseColorWithAlpha(color: string, alpha: number, darkMode?: boolean): string;
/**
 * Check if a string is a valid, parseable color.
 *
 * Returns true for hex, rgb/rgba, oklch, hsl, named colors, and "transparent".
 */
declare function isValidColor(s: string): boolean;

/**
 * Generate optimal darkest and lightest colors for a given hue using practical UI guidelines
 *
 * @param hue hue in degrees 0-360
 * @param chromaBackoff percentage to back off from maximum chroma to avoid edge clipping (0.05 = 5%)
 * @returns object with darkest and lightest colors in 0xrrggbbaa format
 */
declare function getOptimalHueRange(hue: number, chromaBackoff?: number): {
    darkest: number;
    lightest: number;
};
/**
 * Return a shade of the color.
 *
 * The index is a value between 0 and 1000. 0 represent the darkest shade (black) and 1000 represent the lightest shade (white).
 *
 * If color is a single number, the darkest shade and lightest shade are determined based on the hue and chroma of the color, with a 10% lightness for the darkest shade and a 95% lightness for the lightest shade.
 *
 * If `mid` is not provided, it is linearly interpolated between the darkest
 * and lightest shades.
 *
 * The result is determined by interpolating along a s-curve between the darkest and lightest shades, with the `mid` value at index 500.
 *
 * All colors are packed 0xRRGGBBAA integers.
 *
 * @todo: provide a LaTeX equation of the s-curve interpolation function used
 *
 */
declare function shade(color: number | {
    lightest: number;
    darkest: number;
    mid?: number;
}, index: number): number;

/**
 * APCA contrast calculation using Color objects.
 *
 * Return the contrast value (positive for dark text on light background, negative for light text on dark background)
 */
declare function apca(bgColor: Color, fgColor: Color): number;
/**
 * Choose the foreground color with better contrast against the background
 * Returns the color (as rgba number) that provides better APCA contrast
 */
declare function contrastingColor(arg: {
    bg: Color;
    fg1: Color;
    fg2: Color;
} | Color | number): number;

/**
 * Perceptual color difference (ΔE_OK) between two OKLab colors.
 *
 * Computed as the Euclidean distance in OKLab space, which is approximately
 * perceptually uniform — equal numerical differences correspond to
 * approximately equal perceived differences.
 *
 * Typical interpretation:
 * - `< 0.01` — imperceptible
 * - `~ 0.02` — just noticeable
 * - `~ 0.05` — clearly different
 * - `> 0.1`  — very different
 *
 * Alpha is ignored.
 */
declare function oklabDeltaE(a: OklabColor, b: OklabColor): number;
/**
 * Perceptual color difference (ΔE_OK) between two colors, accepting any
 * color form: packed `0xRRGGBBAA` integer, hex string, `RgbColor`,
 * `OklchColor`, or `OklabColor`.
 *
 * Each input is converted to OKLab and the Euclidean distance is computed.
 * See {@link oklabDeltaE} for interpretation of the returned value.
 *
 * Alpha is ignored.
 */
declare function colorDeltaE(a: number | Color, b: number | Color): number;

declare function gray(percent: number): number;
declare function lighten(color: number, amount: number): number;
declare function darken(color: number, amount: number): number;
/**
 * Darken a color by a factor in OKLCH space (perceptually uniform).
 *
 * @param color - Any parseable color string
 * @param factor - Multiplier for lightness (0.6 = 60% of original lightness)
 * @returns `#rrggbb` hex string
 */
declare function darkenHex(color: HexColor, factor: number): HexColor;
/**
 * Determine whether a color is "light" (i.e., needs dark text on top).
 *
 * Uses APCA contrast: if a color has higher contrast with black text than
 * white text, it's considered light.
 */
declare function isLightColor(color: HexColor): boolean;
/**
 * Linearly interpolate between two `[0-1, 0-1, 0-1]` RGB tuples.
 */
declare function mixColors(a: [number, number, number], b: [number, number, number], t: number): [number, number, number];
/**
 * Mix a `[0-1, 0-1, 0-1]` RGB tuple toward white (positive lift) or
 * black (negative lift).
 *
 * @param color - RGB tuple with values in 0-1 range
 * @param lift - Amount to lift: positive = toward white, negative = toward black
 */
declare function liftColor(color: [number, number, number], lift: number): [number, number, number];
/**
 * Convert a hex color string to a CSS color string suitable for Canvas or SVG.
 *
 * - `#RRGGBB` is returned as-is.
 * - `#RRGGBBAA` is converted to `rgba(r, g, b, a)`.
 */
/** Interpolate two OKLCh colors at fraction f in [0,1], returning {L, C, H}. */
declare function lerpOklch(c1: OklchColor, c2: OklchColor, f: number): OklchColor;
/** Interpolate between two hex colors in OKLCh space at fraction f in [0,1]. */
declare function interpolateOklch(hex1: HexColor, hex2: HexColor, f: number): RgbColor;
declare function colorToCss(color: string): string;

type ColorScaleMode = "single" | "two" | "three";
interface ColorScaleStop {
    /** Lightness component in OKLCH (percentage 0-100). */
    lightness: number;
    /** Chroma component in OKLCH (approx 0-0.4 in practice). */
    chroma: number;
    /** Hue component in OKLCH (degrees 0-360). */
    hue: number;
}
interface ColorScalePreset {
    id: string;
    name: string;
    colors: {
        mid?: ColorScaleStop | undefined;
        darkest?: ColorScaleStop | undefined;
        lightest?: ColorScaleStop | undefined;
    };
}
declare const COLOR_SCALE_PRESETS: readonly [{
    readonly id: "pink";
    readonly name: "Pink";
    readonly colors: {
        readonly mid: {
            readonly lightness: 69;
            readonly chroma: 0.202;
            readonly hue: 7.4;
        };
    };
}, {
    readonly id: "red";
    readonly name: "Red";
    readonly colors: {
        readonly lightest: {
            readonly lightness: 95;
            readonly chroma: 0.2;
            readonly hue: 30;
        };
        readonly mid: {
            readonly lightness: 62;
            readonly chroma: 0.25;
            readonly hue: 30;
        };
        readonly darkest: {
            readonly lightness: 36;
            readonly chroma: 0.16;
            readonly hue: 25;
        };
    };
}, {
    readonly id: "brown";
    readonly name: "Brown";
    readonly colors: {
        readonly lightest: {
            readonly lightness: 94.5;
            readonly chroma: 0.021;
            readonly hue: 72.1;
        };
        readonly mid: {
            readonly lightness: 59;
            readonly chroma: 0.2;
            readonly hue: 73;
        };
        readonly darkest: {
            readonly lightness: 20;
            readonly chroma: 0.061;
            readonly hue: 72.7;
        };
    };
}, {
    readonly id: "orange";
    readonly name: "Orange";
    readonly colors: {
        readonly lightest: {
            readonly lightness: 95;
            readonly chroma: 0.06;
            readonly hue: 82;
        };
        readonly mid: {
            readonly lightness: 73;
            readonly chroma: 0.21;
            readonly hue: 56;
        };
        readonly darkest: {
            readonly lightness: 46;
            readonly chroma: 0.2;
            readonly hue: 35;
        };
    };
}, {
    readonly id: "yellow";
    readonly name: "Yellow";
    readonly colors: {
        readonly lightest: {
            readonly lightness: 97;
            readonly chroma: 0.02;
            readonly hue: 106;
        };
        readonly mid: {
            readonly lightness: 89;
            readonly chroma: 0.21;
            readonly hue: 99;
        };
        readonly darkest: {
            readonly lightness: 67;
            readonly chroma: 0.43;
            readonly hue: 59;
        };
    };
}, {
    readonly id: "lime";
    readonly name: "Lime";
    readonly colors: {
        readonly mid: {
            readonly lightness: 65;
            readonly chroma: 0.192;
            readonly hue: 134.3;
        };
    };
}, {
    readonly id: "green";
    readonly name: "Green";
    readonly colors: {
        readonly mid: {
            readonly lightness: 64;
            readonly chroma: 0.21;
            readonly hue: 144;
        };
    };
}, {
    readonly id: "teal";
    readonly name: "Teal";
    readonly colors: {
        readonly mid: {
            readonly lightness: 65;
            readonly chroma: 0.116;
            readonly hue: 192.6;
        };
    };
}, {
    readonly id: "cyan";
    readonly name: "Cyan";
    readonly colors: {
        readonly lightest: {
            readonly lightness: 97;
            readonly chroma: 0.11;
            readonly hue: 195;
        };
        readonly mid: {
            readonly lightness: 61;
            readonly chroma: 0.11;
            readonly hue: 210;
        };
        readonly darkest: {
            readonly lightness: 42;
            readonly chroma: 0.11;
            readonly hue: 210;
        };
    };
}, {
    readonly id: "blue";
    readonly name: "Blue";
    readonly colors: {
        readonly lightest: {
            readonly lightness: 91;
            readonly chroma: 0.22;
            readonly hue: 240;
        };
        readonly mid: {
            readonly lightness: 63;
            readonly chroma: 0.22;
            readonly hue: 255;
        };
        readonly darkest: {
            readonly lightness: 38;
            readonly chroma: 0.22;
            readonly hue: 252;
        };
    };
}, {
    readonly id: "indigo";
    readonly name: "Indigo";
    readonly colors: {
        readonly lightest: {
            readonly lightness: 92.7;
            readonly chroma: 0.036;
            readonly hue: 291;
        };
        readonly mid: {
            readonly lightness: 67;
            readonly chroma: 0.285;
            readonly hue: 296;
        };
        readonly darkest: {
            readonly lightness: 28;
            readonly chroma: 0.23;
            readonly hue: 278;
        };
    };
}, {
    readonly id: "purple";
    readonly name: "Purple";
    readonly colors: {
        readonly mid: {
            readonly lightness: 67;
            readonly chroma: 0.21;
            readonly hue: 299;
        };
    };
}];
type NamedColor = (typeof COLOR_SCALE_PRESETS)[number]["id"];
declare function getColorScaleMode(preset: Pick<ColorScalePreset, "colors">): ColorScaleMode;

type ShadeInput = Parameters<typeof shade>[0];
type ShadeInputMap = Record<string, ShadeInput>;
declare function getLightShadeMap(): ShadeInputMap;
declare function shades(fn: (n: number) => string): {
    [k: string]: string;
};
declare function darkShades(fn: (n: number) => string): {
    [k: string]: string;
};
declare function yellow(n?: number): string;
declare function brown(n?: number): string;
declare function red(n?: number): string;
declare function orange(n?: number): string;
declare function lime(n?: number): string;
declare function green(n?: number): string;
declare function pink(n?: number): string;
declare function purple(n?: number): string;
declare function indigo(n?: number): string;
declare function blue(n?: number): string;
declare function teal(n?: number): string;
declare function cyan(n?: number): string;

declare const CATEGORICAL_PALETTES: {
    readonly tycho11: string[];
    readonly "tycho-dark11": string[];
    readonly "tycho-robust11": string[];
    readonly "tycho-soft11": string[];
    readonly "tycho-soft-dark11": string[];
    readonly "tycho-bold11": string[];
    readonly "tycho-bold-dark11": string[];
    readonly tableau10: string[];
    readonly kelly22: string[];
    readonly mathematica10: string[];
    readonly cupertino10: string[];
    readonly "cupertino-dark10": string[];
    readonly spectrum12: string[];
};
type CategoricalPaletteName = keyof typeof CATEGORICAL_PALETTES;

declare const DIVERGING_PALETTES: {
    readonly roma: string[];
    readonly "roma-reversed": string[];
    readonly vik: string[];
    readonly "vik-reversed": string[];
    readonly broc: string[];
    readonly "broc-reversed": string[];
    readonly rdbu: string[];
    readonly "rdbu-reversed": string[];
    readonly coolwarm: string[];
    readonly "coolwarm-reversed": string[];
};
type DivergingPaletteName = keyof typeof DIVERGING_PALETTES;

declare const SEQUENTIAL_PALETTES: {
    readonly turbo: string[];
    readonly "turbo-reversed": string[];
    readonly grey: string[];
    readonly "grey-reversed": string[];
    readonly magma: string[];
    readonly "magma-reversed": string[];
    readonly inferno: string[];
    readonly "inferno-reversed": string[];
    readonly plasma: string[];
    readonly "plasma-reversed": string[];
    readonly viridis: string[];
    readonly "viridis-reversed": string[];
    readonly cividis: string[];
    readonly "cividis-reversed": string[];
    readonly rocket: string[];
    readonly "rocket-reversed": string[];
    readonly mako: string[];
    readonly "mako-reversed": string[];
    readonly "cmocean-phase": string[];
    readonly "cmocean-phase-reversed": string[];
};
type SequentialPaletteName = keyof typeof SEQUENTIAL_PALETTES;

/** Named design color identifiers shared by foreground and background scales. */
type DesignColorName = keyof typeof BACKGROUND_COLORS;
declare const BACKGROUND_COLORS: {
    readonly red: "#fbbbb6";
    readonly orange: "#ffe0c2";
    readonly yellow: "#fff1c2";
    readonly lime: "#d0e8b9";
    readonly green: "#bceac4";
    readonly teal: "#b9f1f1";
    readonly cyan: "#b8e5c9";
    readonly blue: "#b6d9fb";
    readonly indigo: "#d1c2f0";
    readonly purple: "#e3baf8";
    readonly magenta: "#f9c8e0";
    readonly black: "#353535";
    readonly "dark-grey": "#8C8C8C";
    readonly grey: "#D0D0D0";
    readonly "light-grey": "#F0F0F0";
    readonly white: "#ffffff";
};
declare const FOREGROUND_COLORS: {
    readonly red: "#d7170b";
    readonly orange: "#fe8a2b";
    readonly yellow: "#ffc02b";
    readonly lime: "#63b215";
    readonly green: "#21ba3a";
    readonly teal: "#17cfcf";
    readonly cyan: "#13a7ec";
    readonly blue: "#0d80f2";
    readonly indigo: "#63c";
    readonly purple: "#a219e6";
    readonly magenta: "#eb4799";
    readonly black: "#000";
    readonly "dark-grey": "#666";
    readonly grey: "#A6A6A6";
    readonly "light-grey": "#d4d5d2";
    readonly white: "#ffffff";
};

export { BACKGROUND_COLORS, CATEGORICAL_PALETTES, COLOR_SCALE_PRESETS, type CategoricalPaletteName, type Color, type ColorScaleMode, type ColorScalePreset, type ColorScaleStop, DIVERGING_PALETTES, type DesignColorName, type DivergingPaletteName, FOREGROUND_COLORS, type HexColor, type NamedColor, type OklabColor, type OklchColor, type RgbColor, SEQUENTIAL_PALETTES, type SequentialPaletteName, apca, asColorNumber, asOklch, asRgb, blue, brown, cMaxFor, colorDeltaE, colorToCss, colorToHex, contrastingColor, cyan, darkShades, darken, darkenHex, gammaCorrect, getColorScaleMode, getLightShadeMap, getOptimalHueRange, gray, green, hslToRgb, hsvToRgb, indigo, interpolateOklch, inverseGammaCorrect, isLightColor, isNamedColor, isValidColor, lerpOklch, liftColor, lighten, lime, mixColors, oklabDeltaE, oklabToOklch, oklabToRgb, oklch, oklchFromRGB, oklchToOklab, oklchToRgb, orange, parseColor, parseColorToHex, parseColorToRgb, parseColorToRgb01, parseColorToRgba01, parseColorWithAlpha, parseHexColor, pink, purple, red, rgbToHex, rgbToHsl, rgbToHsv, rgbToOklab, rgbToOklch, shade, shades, teal, toDarkColor, yellow };
